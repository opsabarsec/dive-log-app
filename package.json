{
  "name": "dive-log-app",
  "version": "0.1.0",
  "description": "Dive log application with Convex backend",
  "scripts": {
    "dev": "convex dev",
    "predev": "convex dev --until-success",
    "build": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.17.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.6"
  }
}
